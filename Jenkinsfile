pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                echo 'Your Image has been created'
                echo 'Thanks for using Jenkins'
            }
        }
    }
}
